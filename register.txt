เชื่อมต่อกับ github
git config --global -l 
git config --global user.name "name"
git config --global user.email "mail"

3 0yho9vo 
git add .
git commit 
git push 

git reset --soft HEAD^1
git log
git status
git rm
git restore --staged "file"


เมื่อ commit แล้วอยาก ย้อนกลับ
git reset --soft HEAD^1

save ti git
git remote add origin https://github.com/Khattiya01/git-test.git
git branch -M main
git push -u origin main

สถานะ
git remode -v
.............................................
เปิดอีกไฟล์