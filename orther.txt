git pull https://github.com/Khattiya01/git-test.git main

การสร้าง  branch ใหม่
git branch "namebranch"

เช็ค  ว่ามีกี่ branch
git branch

เปลี่ยน เป็น branch
git checkout "namebranch"
git checkout -b "name"

รวมกัน
git merge "name"
